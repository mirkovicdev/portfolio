// app/quantframe/practice/coding/layout.tsx
// Pyodide is now loaded globally via PyodideProvider in quantframe layout

export default function CodingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
